/**@file common.h
 * @brief Provides common definitions.
 *
 */
#ifndef COMMON_H
#define COMMON_H

#define STR_MAX 13

#define UNUSED(x) (void) x

#define MAX(x, y) (x > y) ? x : y
#define MIN(x, y) (x < y) ? x : y

#endif
